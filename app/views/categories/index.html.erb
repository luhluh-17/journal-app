<h3>Categories</h3>
<div id="categories">
  <% @categories.each do |category| %>
    <h6>
      <%= link_to "#{count_task(category)} #{category.title}", category, class: random_text_bg %>
    </h6>
  <% end %>
</div>
<%= link_to "Add Category", new_category_path, class: "btn btn-primary mt-4" %>
