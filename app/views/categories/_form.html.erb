<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :user_id, class: "form-custom-label"%>
    <%= form.text_field :user_id, value: current_user.id, class: "form-custom-input" %>
  </div>
  <div>
    <%= form.label :title, class: "form-custom-label" %>
    <%= form.text_field :title, class: "form-custom-input" %>
  </div>
  <div class="mt-4">
    <%= form.submit %>
  </div>
<% end %>
